'use client';

// This component is no longer used as ReportsPage is the new dashboard.
// It can be removed in the future.
export default function DashboardHomePageContent({ onNavigate }: { onNavigate: (tab: any) => void; }) {
  return null;
}
